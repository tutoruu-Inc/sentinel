type Review {
    _id: String!
    user: User!
    body: String!
    rating: Int!
    createdAt: String!
}


extend type Query {
    review(_id: String!): Review
    reviews(input: QueryOptions): [Review!]!
}
