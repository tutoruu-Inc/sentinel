type Class {
    _id: String!
    name: String!
    desc: String!
    students: [User!]!
    courses: [Course!]!
    university: University!
}

extend type Query {
    class(_id: String!): Class
    classes(input: QueryOptions): [Class!]!
}
